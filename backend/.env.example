# Django
DJANGO_SECRET_KEY=trhbunndbhdh-jdndbdrcnin
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DJANGO_ENV=development

# Database (PostgreSQL)
DB_NAME=gym_db
DB_USER=gym_user
DB_PASSWORD=gym_user_password
DB_HOST=localhost
DB_PORT=5432

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# JWT
ACCESS_TOKEN_LIFETIME_MINUTES=30
REFRESH_TOKEN_LIFETIME_DAYS=7

# Email 
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=IronForge Gym <no-reply@ironforge.gym>

# Frontend URL (used to build password-reset links)
FRONTEND_URL=http://localhost:5173

DJANGO_SUPERUSER_EMAIL=admin@ironforge.gym
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_PASSWORD=admin
